String methods
Var testString = "Static method which doesn’t require instantiation"
testString.startsWith("");
testString.endsWith("");
testString.includes("");
testString.repeat();

Number.isFinite(Infinity); 
Number.isFinite(3); 
Number.isSafeInteger();
Number.isInteger(222);
